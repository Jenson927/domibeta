export interface AddReason {
  id: number
  name: string
  icon: string
  category: string
  points: number
}

export interface DeductReason {
  id: number
  name: string
  icon: string
  category: string
  points: number
}
